encryption = "age"
[age]
    identity = "~/.config/chezmoi/key.txt"
    recipient = "{{ keyring "chezmoi" "age-recipient" }}"

[git]
    autoCommit = true

[edit]
    command = "code"
    args = ["--wait"]
